openai>=1.0.0
anthropic>=0.39.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
gradio>=4.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional: Browser Research Agent (USE_BROWSER_RESEARCH=1)
# After installing, run: playwright install chromium
playwright>=1.40.0
duckduckgo-search>=6.0.0

# Optional: Observability via Phoenix by Arize (PHOENIX_ENABLED=1) — local dev
# Traces every LLM call + pipeline step → UI at http://localhost:6006
arize-phoenix>=4.0.0
openinference-instrumentation-openai>=0.1.0
openinference-instrumentation-anthropic>=0.1.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# Optional: Logfire tracing (cloud, works in Colab) — get token at logfire.pydantic.dev
# Free tier: 10M spans/month. Traces every Grok + Claude call with prompts + token counts.
logfire[openai,anthropic]>=3.0.0
